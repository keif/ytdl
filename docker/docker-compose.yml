services:
  ytdl:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    image: ytdl:local
    container_name: ytdl
    restart: unless-stopped
    ports:
      - "8766:8766"
    volumes:
      - ~/Videos/ytdl:/downloads
      - ./data:/data
    environment:
      - YTDL_WORKERS=2
